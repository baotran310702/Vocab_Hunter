import 'package:flutter/material.dart';

class ItemHistoryVocabulary extends StatelessWidget {
  const ItemHistoryVocabulary({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      width: MediaQuery.of(context).size.width,
      height: 120,
    );
  }
}
